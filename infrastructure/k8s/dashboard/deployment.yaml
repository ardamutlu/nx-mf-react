apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-deployment
  namespace: mf-workspace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      containers:
        - name: dashboard
          image: ardamutlu/dashboard:latest
          ports:
            - containerPort: 80
          env:
            - name: host_redirect
              valueFrom:
                configMapKeyRef:
                  name: global-mf-config
                  key: host_redirect
          volumeMounts:
            - mountPath: /etc/nginx/conf.d/default.conf.template
              subPath: default.conf.template
              name: nginx-config
      volumes:
        - name: nginx-config
          configMap:
            name: dashboard-nginx-config
